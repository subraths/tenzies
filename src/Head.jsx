const Head = () => {
  return (
    <header>
      <h1>Tenzies</h1>
      <p>
        Roll until all dice are same. Click each die to freeze it at its current
        value between rolls
      </p>
    </header>
  );
};

export default Head;
